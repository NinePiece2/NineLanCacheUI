name: Build API Docker Release CI

on:
  release:
    # Run when a release is published (use the release's tag_name)
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Login to Docker Hub
        uses: docker/login-action@v4.2.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Login to Github Container Registry
        uses: docker/login-action@v4.2.0
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          driver: docker-container

      - name: Build and push
        uses: docker/build-push-action@v7.2.0
        with:
          context: ./NineLanCacheUI-API
          file: ./NineLanCacheUI-API/NineLanCacheUI-API/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/nine-lancache-ui:api-${{ github.event.release.tag_name }}
            ${{ secrets.DOCKER_USERNAME }}/nine-lancache-ui:api
            ghcr.io/ninepiece2/ninelancacheui:api-${{ github.event.release.tag_name }}
            ghcr.io/ninepiece2/ninelancacheui:api
          build-args: |
            VERSION=${{ github.event.release.tag_name }}
            BUILD_DATE=${{ github.event.release.created_at }}
            VCS_REF=${{ github.sha }}
